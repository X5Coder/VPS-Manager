// Package version is the single source of truth for the panel version.
// Bump on every change; it is shown at the bottom of the sidebar.
package version

// Version is the current panel release (sidebar footer + /api/version).
const Version = "v8"
